import wollok.game.*

object positionPrecedence{
  var property position = game.at(1,1)
  method position() = game.at(3,3)
  method image() = "pepita.png"
}

program mockGame {
  game.width(10)
  game.height(10)
  game.addVisual(positionPrecedence)
  game.start()
}